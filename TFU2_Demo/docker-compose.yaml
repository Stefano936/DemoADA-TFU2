services:
  api:
    build: ./api
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml

  notification-service:
    build: ./notification-service
    ports:
      - "8081:8081"
